name: Generate functions
on: push
jobs:
    generate:
        runs-on: ubuntu-latest
        steps:
            - run: printenv | grep '_FUNC'
              env:
                FORMAT_FUNC: ${{format('{0}, Welcome to the {1}','Hi','world')}}
                JOIN_FUNC: ${{join(github.event.commits.message, ', ') }}
