name: Missing shared data
on: push
jobs:
    share:
        runs-on: ubuntu-latest
        steps:
            - run: export RESULT="hello"
            - run: echo "${RESULT}" # Doe not show the RESULT value here.
